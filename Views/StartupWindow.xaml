<Window x:Class="ETAG_ERP.Views.StartupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="ETAG ERP - نظام إدارة المؤسسات" 
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <Style x:Key="StartupCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Background -->
        <Rectangle Fill="#F8F9FA"/>
        
        <!-- Main Card -->
        <Border Style="{StaticResource StartupCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                    <TextBlock Text="🏢" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="ETAG ERP" 
                               FontSize="32" FontWeight="Bold" 
                               Foreground="#2C3E50" 
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="نظام إدارة المؤسسات المتكامل" 
                               FontSize="16" 
                               Foreground="#7F8C8D" 
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Content -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Loading Animation -->
                    <ProgressBar x:Name="LoadingProgressBar" 
                                 Width="300" Height="6" 
                                 Margin="0,0,0,20"
                                 Foreground="#3498DB"/>
                    
                    <!-- Status Text -->
                    <TextBlock x:Name="StatusText" 
                               Text="جاري تحميل النظام..." 
                               FontSize="16" 
                               Foreground="#2C3E50" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,30"/>
                    
                    <!-- Features Grid -->
                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Feature 1 -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center" Margin="10">
                            <TextBlock Text="📊" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="إدارة المبيعات" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="فواتير ومبيعات" FontSize="12" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Feature 2 -->
                        <StackPanel Grid.Column="1" HorizontalAlignment="Center" Margin="10">
                            <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="إدارة المخزون" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="أصناف ومخزون" FontSize="12" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Feature 3 -->
                        <StackPanel Grid.Column="2" HorizontalAlignment="Center" Margin="10">
                            <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="إدارة العملاء" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="عملاء وموردين" FontSize="12" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Footer -->
                <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <!-- Theme Toggle -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="🌙" FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ToggleButton x:Name="ThemeToggleButton" 
                                      Content="الوضع الداكن" 
                                      Width="120" Height="30"
                                      Click="ThemeToggleButton_Click"/>
                        <TextBlock Text="☀️" FontSize="16" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                    
                    <!-- Version Info -->
                    <TextBlock Text="الإصدار 1.0.0 - جميع الحقوق محفوظة" 
                               FontSize="12" 
                               Foreground="#95A5A6" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
