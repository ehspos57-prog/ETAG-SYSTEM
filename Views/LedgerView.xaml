<UserControl x:Class="ETAG_ERP.Views.LedgerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White"
             FontFamily="Segoe UI"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style x:Key="LedgerCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="📊 دفتر الأستاذ" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Filters and Controls -->
        <Border Style="{StaticResource LedgerCard}" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Account Filter -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="الحساب" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="AccountComboBox" 
                              materialDesign:HintAssist.Hint="اختر الحساب"
                              SelectedItem="{Binding SelectedAccount}">
                        <ComboBoxItem Content="جميع الحسابات" IsSelected="True"/>
                    </ComboBox>
                </StackPanel>

                <!-- Date Range -->
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="من تاريخ" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker x:Name="FromDatePicker" 
                                materialDesign:HintAssist.Hint="من تاريخ"
                                SelectedDate="{Binding FromDate}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="إلى تاريخ" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <DatePicker x:Name="ToDatePicker" 
                                materialDesign:HintAssist.Hint="إلى تاريخ"
                                SelectedDate="{Binding ToDate}"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="🔍 بحث" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#3498DB" 
                            Foreground="White"
                            Margin="5,0"
                            Click="SearchButton_Click"/>
                    <Button Content="📄 طباعة" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#E74C3C" 
                            Foreground="White"
                            Margin="5,0"
                            Click="PrintButton_Click"/>
                    <Button Content="📊 تصدير Excel" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#27AE60" 
                            Foreground="White"
                            Margin="5,0"
                            Click="ExportExcelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Ledger Entries Grid -->
        <Border Style="{StaticResource LedgerCard}" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Summary Cards -->
                <UniformGrid Columns="4" Grid.Row="0" Margin="0,0,0,15">
                    <Border Background="#E8F5E8" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="إجمالي المدين" FontWeight="Bold" Foreground="#27AE60"/>
                            <TextBlock x:Name="TotalDebitText" Text="0.00" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFE8E8" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="إجمالي الدائن" FontWeight="Bold" Foreground="#E74C3C"/>
                            <TextBlock x:Name="TotalCreditText" Text="0.00" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#E8F4FD" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="الرصيد" FontWeight="Bold" Foreground="#3498DB"/>
                            <TextBlock x:Name="BalanceText" Text="0.00" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFF3E0" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="عدد القيود" FontWeight="Bold" Foreground="#F39C12"/>
                            <TextBlock x:Name="EntriesCountText" Text="0" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- DataGrid -->
                <DataGrid x:Name="LedgerDataGrid" 
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#F8F9FA"
                          RowHeight="35">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat='yyyy-MM-dd'}" Width="100"/>
                        <DataGridTextColumn Header="رقم القيد" Binding="{Binding EntryNumber}" Width="100"/>
                        <DataGridTextColumn Header="الحساب" Binding="{Binding AccountName}" Width="200"/>
                        <DataGridTextColumn Header="البيان" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="مدين" Binding="{Binding Debit, StringFormat='{}{0:N2}'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="دائن" Binding="{Binding Credit, StringFormat='{}{0:N2}'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#27AE60"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat='{}{0:N2}'}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Add Entry Button -->
        <Button Grid.Row="3" 
                Content="➕ إضافة قيد محاسبي" 
                Style="{StaticResource MaterialDesignRaisedButton}"
                Background="#9B59B6" 
                Foreground="White"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Click="AddEntryButton_Click"/>
    </Grid>
</UserControl>