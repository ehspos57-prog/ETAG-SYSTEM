<UserControl x:Class="ETAG_ERP.Views.ItemsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White"
             FontFamily="Segoe UI"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <Style x:Key="ItemCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="📦 إدارة الأصناف" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Search and Filters -->
        <Border Style="{StaticResource ItemCard}" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="البحث" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox x:Name="SearchTextBox" 
                             materialDesign:HintAssist.Hint="ابحث في الأصناف"
                             TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>

                <!-- Category Filter -->
                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="التصنيف" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CategoryComboBox" 
                              materialDesign:HintAssist.Hint="اختر التصنيف"
                              SelectionChanged="CategoryComboBox_SelectionChanged">
                        <ComboBoxItem Content="جميع التصنيفات" IsSelected="True"/>
                    </ComboBox>
                </StackPanel>

                <!-- Status Filter -->
                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="الحالة" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox x:Name="StatusComboBox" 
                              materialDesign:HintAssist.Hint="اختر الحالة"
                              SelectionChanged="StatusComboBox_SelectionChanged">
                        <ComboBoxItem Content="جميع الحالات" IsSelected="True"/>
                        <ComboBoxItem Content="نشط"/>
                        <ComboBoxItem Content="غير نشط"/>
                    </ComboBox>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="➕ إضافة صنف" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#27AE60" 
                            Foreground="White"
                            Margin="5,0"
                            Click="AddItemButton_Click"/>
                    <Button Content="📊 تصدير" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#3498DB" 
                            Foreground="White"
                            Margin="5,0"
                            Click="ExportItemsButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Items Grid -->
        <Border Style="{StaticResource ItemCard}" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Summary -->
                <UniformGrid Columns="4" Grid.Row="0" Margin="0,0,0,15">
                    <Border Background="#E8F5E8" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="إجمالي الأصناف" FontWeight="Bold" Foreground="#27AE60"/>
                            <TextBlock x:Name="TotalItemsText" Text="0" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#E8F4FD" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="قيمة المخزون" FontWeight="Bold" Foreground="#3498DB"/>
                            <TextBlock x:Name="StockValueText" Text="0.00" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFF3E0" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="أصناف منخفضة" FontWeight="Bold" Foreground="#F39C12"/>
                            <TextBlock x:Name="LowStockText" Text="0" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#FFE8E8" CornerRadius="5" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="أصناف نافدة" FontWeight="Bold" Foreground="#E74C3C"/>
                            <TextBlock x:Name="OutOfStockText" Text="0" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- DataGrid -->
                <DataGrid x:Name="ItemsDataGrid" 
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#F8F9FA"
                          RowHeight="35"
                          SelectionChanged="ItemsDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الكود" Binding="{Binding ItemCode}" Width="100"/>
                        <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ItemName}" Width="200"/>
                        <DataGridTextColumn Header="التصنيف" Binding="{Binding CategoryName}" Width="150"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding StockQuantity}" Width="80"/>
                        <DataGridTextColumn Header="سعر البيع" Binding="{Binding SellingPrice1, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="الوحدة" Binding="{Binding Unit}" Width="80"/>
                        <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="80"/>
                        <DataGridTemplateColumn Header="إجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" Width="30" Margin="2" Click="EditItemButton_Click"/>
                                        <Button Content="🗑️" Width="30" Margin="2" Click="DeleteItemButton_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="🔄 تحديث" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#95A5A6" 
                    Foreground="White"
                    Margin="5,0"
                    Click="RefreshButton_Click"/>
            <Button Content="📄 طباعة" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#8E44AD" 
                    Foreground="White"
                    Margin="5,0"
                    Click="PrintItemsButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>